export type OperatorAsset={id:string;asset_code:string;name:string;asset_name?:string|null;status:string;operational_role?:string|null;category?:{name?:string}|null;activeTicket?:{id:string;work_order_number?:string;started_at?:string;reason?:string}|null};
export function operatorDueLabel(dueAt:string,now=Date.now()){const days=Math.ceil((new Date(dueAt).getTime()-now)/86400000);return days<0?"OVERDUE":days===0?"DUE TODAY":`DUE IN ${days} DAY${days===1?"":"S"}`}
export function operatorFrequency(value:number,unit:string){return`Every ${value} ${unit}${value===1?"":"s"}`}
export function operatorGroup(a:OperatorAsset){const code=a.asset_code.toUpperCase(),cat=String(a.category?.name||"").toUpperCase();if(code.startsWith("DTG-")||cat.includes("DTG"))return"DTG";if(code.startsWith("DRY-")||cat.includes("DRY")||cat.includes("TUNNEL"))return"DRYERS / TUNNELS";if(code.startsWith("SCR-")||cat.includes("SCREEN"))return"SCREEN PRINT";if(code.startsWith("UV")||cat.includes("UV"))return"UV";if(a.operational_role==="UTILITY")return"UTILITIES";if(a.operational_role==="PRODUCTION")return"OTHER PRODUCTION";return"OTHER EQUIPMENT"}
export function groupOperatorAssets(assets:OperatorAsset[]){const order=["DTG","DRYERS / TUNNELS","SCREEN PRINT","UV","OTHER PRODUCTION","UTILITIES","OTHER EQUIPMENT"],groups=new Map<string,OperatorAsset[]>();for(const a of assets){const k=operatorGroup(a);groups.set(k,[...(groups.get(k)||[]),a])}return order.filter(k=>groups.has(k)).map(name=>({name,assets:groups.get(name)!}))}