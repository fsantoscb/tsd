import{AppShell}from"@/components/app-shell";import{DailyPlanBuilder}from"@/components/daily-plan-builder";import{dailyPlan}from"@/lib/daily-plans";import{transitionPlan,updateItem}from"../actions";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params,d=await dailyPlan(id),edit=d.plan.status==="draft";return <AppShell><p className="eyebrow">Daily plan · {d.plan.status}</p><header><div><h2>{d.plan.area_code}<br/>{d.plan.shift_name}.</h2><p>{d.plan.production_date} · version {d.plan.version}</p></div><form action={transitionPlan}><input type="hidden" name="plan_id" value={id}/>{d.plan.status==="draft"&&<button name="action" value="publish" disabled={!d.items.length}>Publish plan</button>}{d.plan.status==="published"&&<button name="action" value="close">Close plan</button>}</form></header>{edit&&<DailyPlanBuilder planId={id} orders={d.eligible}/>}<section className="sequence"><h3>Execution sequence <small>{d.items.length} orders</small></h3>{d.items.map((i:any)=><form action={updateItem} key={i.id}><input type="hidden" name="plan_id" value={id}/><input type="hidden" name="item_id" value={i.id}/><input name="sequence" type="number" min="1" defaultValue={i.sequence} disabled={!edit}/><b>{i.order_no}</b><input name="planned_units" type="number" min=".001" step=".001" defaultValue={i.planned_units} disabled={!edit}/><input name="responsible" defaultValue={i.responsible??""} placeholder="Responsible" disabled={!edit}/><span>{i.special_instruction??"No special instruction"}</span>{edit&&<button>Save</button>}</form>)}</section><section className="panel"><h3>Plan history</h3>{d.history.map((h:any)=><p key={h.changed_at}><b>{h.action}</b> · {h.changed_by_email||"system"} · {new Date(h.changed_at).toLocaleString("en-AU")}</p>)}</section></AppShell>}
