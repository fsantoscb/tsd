import{AppShell}from"@/components/app-shell";import{deputyContext}from"@/lib/deputy";import{uploadDeputy}from"./actions";export default async function Page({searchParams}:{searchParams:Promise<{result?:string}>}){const[p,d]=await Promise.all([searchParams,deputyContext()]);return <AppShell><p className="eyebrow">ERP KPI · Phase B</p><header><div><h2>Deputy labour<br/>imports.</h2><p>Raw, auditable timesheets. Invalid rows remain quarantined and cannot affect KPIs.</p></div></header>{p.result&&<p className="notice">{p.result==="duplicate"?"This exact file was already imported. No hours were duplicated.":"Deputy file imported and validated."}</p>}<section className="panel"><h3>Import Deputy export</h3><form action={uploadDeputy} className="deputy-upload"><input name="file" type="file" accept=".csv,.xlsx" required/><select name="timezone" defaultValue="Australia/Brisbane"><option>Australia/Brisbane</option><option>UTC</option></select><button type="submit" className="deputy-submit">Validate and import</button></form><small>CSV or XLSX, maximum 15 MB. Select UTC only when the exported timestamps are UTC.</small></section><section className="panel"><h3>Import history</h3><div className="table"><table><thead><tr><th>Imported</th><th>File</th><th>Coverage</th><th>Rows</th><th>Approved</th><th>Provisional</th><th>Quarantined</th><th>Status</th></tr></thead><tbody>{d.batches.map((b:any)=><tr key={b.id}><td>{new Date(b.imported_at).toLocaleString("en-AU",{timeZone:"Australia/Brisbane"})}</td><td>{b.filename}</td><td>{b.covered_from??"-"} → {b.covered_to??"-"}</td><td>{b.raw_rows}</td><td>{b.approved_rows}</td><td>{b.provisional_rows}</td><td>{b.quarantined_rows}</td><td>{b.status}</td></tr>)}</tbody></table></div></section><section className="panel"><h3>Latest quarantined rows</h3><div className="table"><table><thead><tr><th>Date</th><th>Employee</th><th>Area</th><th>Reasons</th></tr></thead><tbody>{d.exceptions.map((e:any)=><tr key={e.id}><td>{e.timesheet_date??"-"}</td><td>{e.display_name??"Missing"}</td><td>{e.raw_area??"Missing"}</td><td>{e.quarantine_reasons.join(", ")}</td></tr>)}</tbody></table></div></section></AppShell>}
