import Link from"next/link";import{AppShell}from"@/components/app-shell";import{dailyPlans}from"@/lib/daily-plans";import{createPlan}from"./actions";export default async function Page(){const d=await dailyPlans();return <AppShell><p className="eyebrow">Phase 6</p><header><div><h2>Daily production<br/>plans.</h2><p>Build, publish and close the executable sequence.</p></div></header><form action={createPlan} className="create-plan"><input name="production_date" type="date" required/><select name="shift_id" required defaultValue=""><option value="" disabled>Shift</option>{d.shifts.map((s:any)=><option key={s.id} value={s.id}>{s.name}</option>)}</select><select name="area_id" required defaultValue=""><option value="" disabled>Area</option>{d.areas.map((a:any)=><option key={a.id} value={a.id}>{a.name}</option>)}</select><button>Create draft</button></form><section className="plan-grid">{d.plans.map((p:any)=><Link href={`/production/plans/${p.id}`} key={p.id}><span className={`plan-status ${p.status}`}>{p.status}</span><h3>{p.area_code} · {p.shift_name}</h3><b>{p.production_date}</b><small>{p.item_count} orders · {Number(p.planned_units).toLocaleString("en-AU")} units</small></Link>)}</section></AppShell>}
