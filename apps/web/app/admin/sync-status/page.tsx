import {AppShell} from "@/components/app-shell";import {FreshnessBadge} from "@/components/freshness-badge";import {syncStatus} from "@/lib/source-data";export default async function Page(){const d=await syncStatus(),latest=d.batches[0];return <AppShell><p className="eyebrow">Integration health</p><header><div><h2>Oracle<br/>connector.</h2><p>Batch history and agent heartbeat.</p></div><FreshnessBadge at={latest?.completed_at??null}/></header><section className="panel"><h3>Agent heartbeat</h3>{d.heartbeats.length?d.heartbeats.map((h:any)=><p key={h.agent_id}>{h.agent_id}: <b>{h.status}</b> at {h.last_seen_at}</p>):<p>No heartbeat received yet.</p>}</section><div className="table"><table><thead><tr><th>Status</th><th>Completed</th><th>Orders</th><th>Workbank</th><th>Stock</th><th>Audit</th></tr></thead><tbody>{d.batches.map((b:any)=><tr key={b.id}><td>{b.status}</td><td>{b.completed_at??"-"}</td><td>{b.orders_count}</td><td>{b.workbank_count}</td><td>{b.stock_count}</td><td>{b.audit_new_count}</td></tr>)}</tbody></table></div></AppShell>}
