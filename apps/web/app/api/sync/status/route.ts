import{NextResponse}from"next/server";import{createClient}from"@/lib/supabase/server";import{getSyncHealth}from"@/lib/sync-health";export const dynamic="force-dynamic";export async function GET(){const c=await createClient(),{data}=await c.auth.getUser();if(!data.user)return NextResponse.json({error:"Unauthorized"},{status:401});try{return NextResponse.json(await getSyncHealth(),{headers:{"Cache-Control":"no-store, no-cache, must-revalidate"}})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Status unavailable"},{status:500})}}
