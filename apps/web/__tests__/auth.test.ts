import {describe,expect,it} from "vitest";import {isAuthorizedAdminEmail,validatePassword} from "../lib/auth";describe("password validation",()=>{it("requires twelve characters",()=>expect(validatePassword("Short1A")).toBeTruthy());it("requires mixed characters",()=>expect(validatePassword("abcdefghijkl")).toBeTruthy());it("accepts a strong password",()=>expect(validatePassword("FactoryShift9")).toBeNull())});describe("admin authorization",()=>{it("fails closed when no allow-list is configured",()=>expect(isAuthorizedAdminEmail("admin@example.com","")).toBe(false));it("matches a normalized comma-separated allow-list",()=>expect(isAuthorizedAdminEmail("ADMIN@example.com","user@example.com, admin@example.com")).toBe(true))});
