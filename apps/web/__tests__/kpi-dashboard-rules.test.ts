import{describe,expect,it}from"vitest";import{backlogClearance,brisbaneDate,capacityGap,capacityLoad,labourProductivity,planAttainment,riskLevel,safeDivide,slaBucket,variance}from"../lib/kpi-dashboard-rules";
describe("production performance KPI rules",()=>{it("separates attainment, variance, load and gap",()=>{expect(planAttainment(90,100)).toBe(.9);expect(variance(90,100)).toBe(-10);expect(capacityLoad(120,100)).toBe(1.2);expect(capacityGap(120,100)).toBe(-20)});it("calculates clearance and labour productivity",()=>{expect(backlogClearance(900,300)).toBe(3);expect(labourProductivity(600,8)).toBe(75)});it("never converts unavailable denominators to zero",()=>{expect(safeDivide(10,0)).toBeNull();expect(planAttainment(10,null)).toBeNull();expect(capacityLoad(10,0)).toBeNull();expect(backlogClearance(10,null)).toBeNull();expect(labourProductivity(10,0)).toBeNull()});it("uses deterministic SLA buckets",()=>{expect([.5,1.5,2.5,3.5,5].map(slaBucket)).toEqual(["0-1","1-2","2-3","3-4",">4"])});it("uses due date, age and source priority for risk",()=>{const now=new Date("2026-09-13T00:00:00Z");expect(riskLevel(7,null,null,now)).toBe("CRITICAL");expect(riskLevel(1,"2026-09-01",null,now)).toBe("HIGH");expect(riskLevel(1,null,1,now)).toBe("CRITICAL");expect(riskLevel(3,null,null,now)).toBe("MEDIUM")});it("uses the Brisbane calendar boundary",()=>{expect(brisbaneDate(new Date("2026-09-12T15:30:00Z"))).toBe("2026-09-13")})});
