import{describe,expect,it}from"vitest";import{canPlanTransition,validSequence,validUnits}from"../lib/daily-plan-rules";describe("daily plans",()=>{it("publishes non-empty drafts",()=>{expect(canPlanTransition("draft","publish",1)).toBe(true);expect(canPlanTransition("draft","publish",0)).toBe(false)});it("closes published plans",()=>{expect(canPlanTransition("published","close",1)).toBe(true);expect(canPlanTransition("draft","close",1)).toBe(false)});it("validates positive values",()=>{expect(validSequence(1)).toBe(true);expect(validSequence(0)).toBe(false);expect(validUnits(.001)).toBe(true);expect(validUnits(0)).toBe(false)})});
