import{describe,expect,it}from"vitest";import{orderPayload,parseScan}from"../lib/scan-rules";describe("operational scan rules",()=>{it("preserves leading zeroes and long IDs as text",()=>{expect(parseScan("ORDER:00001234567890123456"))?.toMatchObject({kind:"ORDER",value:"00001234567890123456"})});it("recognizes TSD order and pack payloads",()=>{expect(parseScan("TSD:ORDER:A-001")?.kind).toBe("ORDER");expect(parseScan("TSD:PACK:PK_900")?.kind).toBe("PACK")});it("keeps unprefixed scanner input in auto mode",()=>{expect(parseScan("001234"))?.toEqual({kind:"AUTO",value:"001234",canonical:"TSD:AUTO:001234"})});it("rejects empty, unsafe and oversized values",()=>{expect(parseScan(" ")).toBeNull();expect(parseScan("ORDER:12,34")).toBeNull();expect(parseScan("x".repeat(129))).toBeNull()});it("builds the approved operational QR payload",()=>{expect(orderPayload("00042")).toBe("TSD:ORDER:00042")})});
