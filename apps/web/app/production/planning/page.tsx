import {AppShell} from "@/components/app-shell";import {PlanningBoard} from "@/components/planning-board";import {planningContext} from "@/lib/planning";
export default async function PlanningPage(){const d=await planningContext(),planned=d.orders.filter((x:any)=>x.planning_status&&x.planning_status!=="unplanned").length;return <AppShell><p className="eyebrow">Phase 5</p><header><div><h2>Production<br/>planning.</h2><p>App-owned commitments. Oracle remains read-only.</p></div><div className="queue-total"><span>Planned orders</span><strong>{planned} / {d.orders.length}</strong></div></header><PlanningBoard orders={d.orders} shifts={d.shifts}/></AppShell>}
