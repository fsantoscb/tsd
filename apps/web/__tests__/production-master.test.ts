import{describe,expect,it}from'vitest';const canonical=['PICKING','DTG_PRINT','UNDERPRINT','PUTWALL','SCREEN_PRINT','UV_PRINT','LAMINATION','CUTTING','QC','PACKING','DISPATCH'];describe('Phase A master data',()=>{it('uses stable canonical operation codes',()=>expect(canonical.every(x=>/^[A-Z0-9_]+$/.test(x))).toBe(true));it('does not use Oracle queue/location values as canonical operations',()=>expect(canonical).not.toEqual(expect.arrayContaining(['SP11','PCOR','PWL1','DTGMOVE','UPMOVE'])));it('preserves explicit unmapped classification',()=>expect(['OTHER','UNMAPPED']).toContain('UNMAPPED'))})
