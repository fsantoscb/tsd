import {describe,expect,it} from "vitest";import {ageBand,normalizeStage} from "../lib/stage-engine";describe("stage engine",()=>{it("accepts known stages and keeps unknown values unmapped",()=>{expect(normalizeStage("DTG")).toBe("DTG");expect(normalizeStage("unknown")).toBe("UNMAPPED")});it("classifies aged orders",()=>{expect(ageBand(2)).toBe("current");expect(ageBand(7)).toBe("warning");expect(ageBand(14)).toBe("critical")})});
