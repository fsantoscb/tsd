import{AppShell}from"@/components/app-shell";import{getSyncHealth}from"@/lib/sync-health";export const dynamic="force-dynamic";const dt=(v:string|null)=>v?new Intl.DateTimeFormat("en-AU",{timeZone:"Australia/Brisbane",dateStyle:"medium",timeStyle:"medium"}).format(new Date(v)):"—";export default async function Page(){const d=await getSyncHealth(),h=d.heartbeat,b=d.batch,r=d.lastRun;return <AppShell><div className="source-health"><p className="eyebrow">Admin / Data</p><header><div><h2>Source Health</h2><p>Oracle connector status without opening server logs.</p></div><strong className={`health-${d.state.toLowerCase()}`}>{d.state}</strong></header><section className="health-grid"><article><span>Agent</span><b>{d.state==="AGENT_OFFLINE"?"OFFLINE":"ONLINE"}</b><small>{h?.agent_id??"—"} · {h?.version??"—"}</small></article><article><span>Last heartbeat</span><b>{dt(h?.last_seen_at??null)}</b><small>{h?.hostname??"—"}</small></article><article><span>Last successful sync</span><b>{dt(d.lastSuccess)}</b><small>Batch {b?.id??"—"}</small></article><article><span>Last attempt</span><b>{r?.status??"—"}</b><small>{dt(r?.started_at??null)}</small></article><article><span>Next expected sync</span><b>{dt(h?.next_expected_sync_at??null)}</b><small>Automatic interval: 5 minutes</small></article><article><span>Last duration</span><b>{r?.duration_ms?`${(Number(r.duration_ms)/1000).toFixed(1)} s`:"—"}</b><small>{r?.trigger_type??"—"}</small></article></section><section className="health-counts"><span>Orders <b>{b?.orders_count??0}</b></span><span>Workbank <b>{b?.workbank_count??0}</b></span><span>Stock <b>{b?.stock_count??0}</b></span><span>Audit <b>{r?.audit_count??b?.audit_new_count??0}</b></span></section><section className="panel"><h3>Recent sync executions</h3><div className="table"><table><thead><tr><th>Trigger</th><th>Status</th><th>Requested</th><th>Duration</th><th>Counts</th><th>Failure</th></tr></thead><tbody>{d.recentRuns.map((x:any)=><tr key={x.id}><td>{x.trigger_type}</td><td>{x.status}</td><td>{dt(x.requested_at)}</td><td>{x.duration_ms?`${(Number(x.duration_ms)/1000).toFixed(1)}s`:"—"}</td><td>{x.orders_count}/{x.workbank_count}/{x.stock_count}/{x.audit_count}</td><td>{x.failure_reason??"—"}</td></tr>)}</tbody></table></div></section></div></AppShell>}
