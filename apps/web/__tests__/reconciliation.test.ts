import {describe,expect,it} from "vitest";import {freshness,normalizePage} from "../lib/reconciliation";describe("reconciliation helpers",()=>{it("normalizes pages",()=>{expect(normalizePage("0")).toBe(1);expect(normalizePage("3")).toBe(3)});it("uses centralized seven/fifteen minute thresholds",()=>{const n=Date.parse("2026-09-08T10:20:00Z");expect(freshness("2026-09-08T10:14:00Z",n).level).toBe("fresh");expect(freshness("2026-09-08T10:10:00Z",n).level).toBe("delayed");expect(freshness("2026-09-08T10:04:59Z",n).level).toBe("stale")})});
