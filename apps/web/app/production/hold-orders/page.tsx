import Link from "next/link";
import {AppShell} from "@/components/app-shell";
import {holdOrders} from "@/lib/hold-orders";
export default async function Page(){const rows=await holdOrders();return <AppShell><section className="operations-hero"><div><p className="eyebrow">WORKBANK EXCEPTIONS · HOLD</p><h2>Hold<br/>orders.</h2><p>Intervention queue for open orders blocked in the Oracle HOLD queue.</p></div><div className="hero-clock"><span>ORDERS ON HOLD</span><strong>{new Set(rows.map(row=>row.order_no)).size}</strong><small>{rows.length} order / location records</small></div></section><div className="table"><table><thead><tr><th>Order</th><th>Client</th><th>Current queue</th><th>Location</th><th>Action</th></tr></thead><tbody>{rows.map((row:any)=><tr key={`${row.order_no}-${row.to_location??""}`}><td><Link className="order-link" href={`/production/orders/${encodeURIComponent(row.order_no)}`}>{row.order_no}</Link></td><td>{row.ship_to_name||"-"}</td><td><span className="hold-chip">HOLD</span></td><td>{row.to_location||"-"}</td><td><Link className="row-action" href={`/production/orders/${encodeURIComponent(row.order_no)}`}>Investigate</Link></td></tr>)}</tbody></table></div>{!rows.length&&<section className="empty-state"><h3>No orders on hold</h3><p>No open 130-series Workbank order is currently assigned to queue HOLD.</p></section>}</AppShell>}
