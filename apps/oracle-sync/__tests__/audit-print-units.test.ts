import{describe,expect,it}from"vitest";import{canonicalDtgPrintUnits,isCanonicalDtgPrint,mapAudit}from"../src/mapping";const base={AUDIT_ID:1,ONO:130,QTY:3,WEIGHT:3,TIMESTAMP:new Date("2026-09-11T00:00:00Z")};describe("Oracle audit metric units",()=>{it("does not treat PCOR alone as canonical DTG output",()=>{const row={...base,QUEUE:"PCOR",ISIS_TASK:"PCOR",FROM_ZONE:"OTHER",TO_ZONE:"OTHER",PROD_X_5:"2"};expect(isCanonicalDtgPrint(row)).toBe(false);expect(canonicalDtgPrintUnits(row)).toBeNull();expect(mapAudit(row).productionUnits).toBe(3)});it("uses DTGS to PWL1 as canonical DTG output and multiplies QTY by PROD_X_5",()=>{const row={...base,FROM_ZONE:"dtgs",TO_ZONE:"pwl1",PROD_X_5:"2"};expect(isCanonicalDtgPrint(row)).toBe(true);expect(canonicalDtgPrintUnits(row)).toBe(6);expect(mapAudit(row).productionUnits).toBe(6)});it("uses one print when PROD_X_5 is missing",()=>expect(mapAudit({...base,FROM_ZONE:"DTGS",TO_ZONE:"PWL1"}).productionUnits).toBe(3));it("reconciles the source-confirmed 09 and 10 September fixtures",()=>{const sum=(rows:Record<string,unknown>[])=>rows.reduce((n,row)=>n+(canonicalDtgPrintUnits(row)??0),0);expect(sum([{...base,FROM_ZONE:"DTGS",TO_ZONE:"PWL1",QTY:3738,PROD_X_5:"1"}])).toBe(3738);expect(sum([{...base,FROM_ZONE:"DTGS",TO_ZONE:"PWL1",QTY:2099,PROD_X_5:"1"}])).toBe(2099)});it("keeps physical garments for unrelated movements",()=>expect(mapAudit({...base,QUEUE:"MOVE",ISIS_TASK:"ATOC",FROM_ZONE:"OTHER",TO_ZONE:"OTHER",PROD_X_5:"2"}).productionUnits).toBe(3))});
