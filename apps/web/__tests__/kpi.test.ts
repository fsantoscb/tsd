import{describe,expect,it}from"vitest";import{quality,safeRatio,status}from"../lib/kpi-rules";
describe("KPI engine",()=>{it("never divides by zero or missing data",()=>{expect(safeRatio(2,0)).toBeNull();expect(safeRatio(null,2)).toBeNull();expect(safeRatio(3,2)).toBe(1.5)});it("resolves higher-is-better thresholds",()=>{expect(status(100,90,80,70,"HIGHER_IS_BETTER")).toBe("GOOD");expect(status(85,90,80,70,"HIGHER_IS_BETTER")).toBe("WARNING");expect(status(60,90,80,70,"HIGHER_IS_BETTER")).toBe("CRITICAL")});it("resolves lower-is-better and no target",()=>{expect(status(3,5,8,10,"LOWER_IS_BETTER")).toBe("GOOD");expect(status(6,null,null,null,"LOWER_IS_BETTER")).toBe("NO_TARGET");expect(status(null,5,8,10,"LOWER_IS_BETTER")).toBe("NO_DATA")});it("marks stale and absent sources",()=>{const now=new Date("2026-09-09T10:00:00Z");expect(quality(null,now)).toBe("NO_DATA");expect(quality("2026-09-09T09:00:00Z",now)).toBe("VALID");expect(quality("2026-09-07T09:00:00Z",now)).toBe("STALE_DATA")})});
