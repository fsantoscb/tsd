import Link from"next/link";import{AppShell}from"@/components/app-shell";import{maintenanceData}from"@/lib/maintenance";import{reportProblem,transitionWO}from"../actions";
export default async function Page(){const d=await maintenanceData();return <AppShell><div className="mx"><header><div><p className="eyebrow">Corrective maintenance</p><h2>Work orders</h2><p>Report, prioritise and close failures.</p></div></header><form id="report" action={reportProblem} className="mx-form report"><select name="asset_id" required defaultValue=""><option value="" disabled>Select asset</option>{d.assets.map((x:any)=><option value={x.id} key={x.id}>{x.asset_code} · {x.name}</option>)}</select><input name="title" placeholder="Problem title" required/><textarea name="problem_description" placeholder="What happened?"/><select name="priority" defaultValue="medium"><option value="low">Low</option><option value="medium">Medium</option><option value="high">High</option><option value="critical">Critical</option></select><label><input type="checkbox" name="machine_stopped" value="yes"/> Machine stopped</label><button>Report problem</button></form><div className="mx-orders">{d.orders.map((x:any)=><article key={x.id}><div><small>{x.work_order_number} · {x.asset?.asset_code}</small><h3><Link href={`/maintenance/work-orders/${x.id}`}>{x.title}</Link></h3><p>{x.problem_description||"No description"}</p><i className={x.priority}>{x.priority}</i></div>{!["completed","cancelled"].includes(x.status)?<form action={transitionWO}><input type="hidden" name="work_order_id" value={x.id}/><select name="status" defaultValue={x.status}>{["open","in_progress","waiting_parts","waiting_external","completed","cancelled"].map(s=><option value={s} key={s}>{s.replaceAll("_"," ")}</option>)}</select><input name="root_cause" defaultValue={x.root_cause??""} placeholder="Root cause"/><input name="resolution" defaultValue={x.resolution??""} placeholder="Resolution required to complete"/><input name="cancel_reason" placeholder="Reason required to cancel"/><button>Update</button></form>:<strong>{x.status}</strong>}</article>)}</div></div></AppShell>}
