import {describe,expect,it} from "vitest";import {cleanFilter,lineFromPath,pageNumber} from "../lib/operational";describe("operational workbanks",()=>{it("normalizes pagination",()=>{expect(pageNumber("2")).toBe(2);expect(pageNumber("-1")).toBe(1)});it("limits and trims filters",()=>expect(cleanFilter(`  ${"a".repeat(100)} `)).toHaveLength(80));it("resolves production line",()=>{expect(lineFromPath("dtg")).toBe("DTG");expect(lineFromPath("other")).toBe("UP")})});
