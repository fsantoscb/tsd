import {describe,expect,it,vi} from "vitest";import {chunkValues,cleanFilter,collectPaged,displayDtgLocation,lineFromPath,pageNumber,workbankLocation} from "../lib/operational";describe("operational workbanks",()=>{it("normalizes pagination",()=>{expect(pageNumber("2")).toBe(2);expect(pageNumber("-1")).toBe(1)});it("limits and trims filters",()=>expect(cleanFilter(`  ${"a".repeat(100)} `)).toHaveLength(80));it("resolves production line",()=>{expect(lineFromPath("dtg")).toBe("DTG");expect(lineFromPath("other")).toBe("UP")});it("uses Workbank From Location until printing starts",()=>{expect(displayDtgLocation(0,"PWL1-12C","PWL1-20B")).toBe("PWL1-12C");expect(displayDtgLocation(1,"PWL1-12C","PWL1-20B")).toBe("PWL1-20B");expect(displayDtgLocation(0,null,"PWL1-10A")).toBe("PWL1-10A");expect(displayDtgLocation(1,"PWL1-12C",null)).toBe("—");expect(displayDtgLocation(0,null,null)).toBe("—")});it("chunks more than 100 DTG orders without loss",()=>{const values=Array.from({length:205},(_,index)=>`order-${index}`),chunks=chunkValues(values,100);expect(chunks.map(chunk=>chunk.length)).toEqual([100,100,5]);expect(chunks.flat()).toEqual(values)});it("retrieves every page beyond the default row limit",async()=>{const source=Array.from({length:2150},(_,index)=>index),read=vi.fn(async(from:number,to:number)=>source.slice(from,to+1));await expect(collectPaged(read,1000)).resolves.toEqual(source);expect(read.mock.calls).toEqual([[0,999],[1000,1999],[2000,2999]])});it("uses non-empty PWL1 location fallback",()=>{expect(workbankLocation(" PWL1-18A ","PWL1")).toBe("PWL1-18A");expect(workbankLocation("   "," PALLET05 ")).toBe("PALLET05");expect(workbankLocation(null," PWL1-22B ")).toBe("PWL1-22B")})});
