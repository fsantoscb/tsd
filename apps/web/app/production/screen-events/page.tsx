import{AppShell}from"@/components/app-shell";import{capacityContext,capacityDb}from"@/lib/capacity";import{recordScreenEvent}from"./actions";const n=(x:unknown)=>Number(x).toLocaleString("en-AU",{maximumFractionDigits:2});export default async function Page(){const c=await capacityContext(),d=capacityDb(),{data,error}=await d.from("production_events").select("event_id,event_ts_local,operational_date,shift_code,metric,quantity,unit,quality_status,source_record_key").eq("organization_id",c.organizationId).eq("area","SCREEN_PRINT").order("event_ts_utc",{ascending:false}).limit(100);if(error)throw error;return <AppShell><div className="erp-kpi"><header><div><p>SCREEN PRINT · MANUAL SOURCE</p><h2>Production events</h2><span>Record output and machine-hours with a shared auditable event key.</span></div></header><form action={recordScreenEvent} className="screen-event-form"><label>Local event time<input required type="datetime-local" name="eventAt"/></label><label>Garments<input required min="0.01" step="0.01" type="number" name="garments"/></label><label>Machine-hours<input min="0" step="0.01" type="number" name="machineHours" defaultValue="0"/></label><button>Record event</button></form><section className="erp-table"><table><thead><tr><th>Local time</th><th>Operational date</th><th>Shift</th><th>Metric</th><th>Quantity</th><th>Unit</th><th>Quality</th><th>Trace key</th></tr></thead><tbody>{(data??[]).map((x:any)=><tr key={x.event_id}><th>{String(x.event_ts_local).replace("T"," ")}</th><td>{x.operational_date}</td><td>{x.shift_code}</td><td>{x.metric}</td><td>{n(x.quantity)}</td><td>{x.unit}</td><td>{x.quality_status}</td><td>{x.source_record_key}</td></tr>)}</tbody></table></section></div></AppShell>}
