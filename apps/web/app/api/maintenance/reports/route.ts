import{NextRequest}from"next/server";import{maintenanceMetrics}from"@/lib/maintenance";const esc=(v:any)=>`"${String(v??"").replaceAll('"','""')}"`;export async function GET(req:NextRequest){const now=new Date(),from=req.nextUrl.searchParams.get("from")||new Date(now.getTime()-29*86400000).toISOString().slice(0,10),to=req.nextUrl.searchParams.get("to")||now.toISOString().slice(0,10),d=await maintenanceMetrics(from,to),rows=[["Work Order ID","Asset","Type","Status","Priority","Requested","Completed"],...d.orders.map((x:any)=>[x.id,x.asset?.asset_code,x.work_order_type,x.status,x.priority,x.requested_at,x.completed_at])],csv='\ufeff'+rows.map(r=>r.map(esc).join(',')).join('\r\n');return new Response(csv,{headers:{"content-type":"text/csv; charset=utf-8","content-disposition":`attachment; filename="maintenance-${from}-${to}.csv"`}})}