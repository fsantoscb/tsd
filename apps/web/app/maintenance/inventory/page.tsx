import{AppShell}from"@/components/app-shell";import{inventoryData}from"@/lib/maintenance";import{createInventoryLocation,createPart,postInventory}from"../actions";const n=(v:any)=>new Intl.NumberFormat("en-AU",{maximumFractionDigits:2}).format(Number(v||0));const money=(v:any)=>new Intl.NumberFormat("en-AU",{style:"currency",currency:"AUD"}).format(Number(v||0));
export default async function Page(){const d=await inventoryData(),low=d.stock.filter((x:any)=>Number(x.stock_on_hand)<=Number(x.reorder_point));return <AppShell><div className="mx"><header className="mx-hero"><div><p className="eyebrow">Maintenance inventory</p><h2>Parts under control.</h2><p>Transaction-led stock with work-order cost traceability.</p></div></header><section className="mx-kpis"><article><span>Active parts</span><strong>{d.parts.length}</strong></article><article><span>Locations</span><strong>{d.locations.length}</strong></article><article className="risk"><span>Low stock</span><strong>{low.length}</strong></article><article><span>Inventory value</span><strong>{money(d.stock.reduce((a:number,x:any)=>a+Number(x.stock_on_hand)*Number(x.unit_cost),0))}</strong></article></section><section className="mx-grid"><form action={createPart} className="mx-form"><h3>Add part</h3><input name="part_number" placeholder="Part number" required/><input name="description" placeholder="Description" required/><input name="manufacturer" placeholder="Manufacturer"/><input name="unit_cost" type="number" min="0" step="0.01" placeholder="Unit cost" required/><input name="reorder_point" type="number" min="0" step="0.01" placeholder="Minimum stock" required/><button>Add part</button></form><form action={createInventoryLocation} className="mx-form"><h3>Add stock location</h3><input name="name" placeholder="Location name" required/><input name="code" placeholder="Code"/><button>Add location</button></form></section><form action={postInventory} className="mx-form mx-stock-form"><select name="part_id" required defaultValue=""><option value="" disabled>Part</option>{d.parts.map((x:any)=><option value={x.id} key={x.id}>{x.part_number} · {x.description}</option>)}</select><select name="location_id" required defaultValue=""><option value="" disabled>Location</option>{d.locations.map((x:any)=><option value={x.id} key={x.id}>{x.name}</option>)}</select><select name="transaction_type" defaultValue="receipt"><option value="receipt">Receipt</option><option value="issue">Issue to work order</option><option value="return">Return</option><option value="adjustment">Positive adjustment</option></select><select name="work_order_id" defaultValue=""><option value="">No work order</option>{d.orders.map((x:any)=><option value={x.id} key={x.id}>{x.work_order_number} · {x.title}</option>)}</select><input name="quantity" type="number" min="0.01" step="0.01" placeholder="Quantity" required/><input name="unit_cost" type="number" min="0" step="0.01" placeholder="Unit cost" required/><input name="notes" placeholder="Notes"/><button>Post transaction</button></form><div className="mx-table"><table><thead><tr><th>Part</th><th>Description</th><th>Location</th><th>On hand</th><th>Minimum</th><th>Unit cost</th><th>Status</th></tr></thead><tbody>{d.stock.map((x:any)=><tr key={`${x.part_id}-${x.location_id}`}><td><b>{x.part_number}</b></td><td>{x.description}</td><td>{x.location}</td><td>{n(x.stock_on_hand)}</td><td>{n(x.reorder_point)}</td><td>{money(x.unit_cost)}</td><td><i className={Number(x.stock_on_hand)<=Number(x.reorder_point)?"critical":"operational"}>{Number(x.stock_on_hand)<=Number(x.reorder_point)?"reorder":"healthy"}</i></td></tr>)}</tbody></table></div></div></AppShell>}