import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{resolve}from"node:path";import{roleHasPermission}from"../lib/permissions";
describe("canonical authorization",()=>{it("enforces role isolation",()=>{expect(roleHasPermission("operator","MAINTENANCE_REOPEN")).toBe(false);expect(roleHasPermission("supervisor","MAINTENANCE_REOPEN")).toBe(true);expect(roleHasPermission("supervisor","CAPACITY_CONFIGURE")).toBe(false);expect(roleHasPermission("manager","CAPACITY_CONFIGURE")).toBe(true);expect(roleHasPermission("manager","USER_ADMIN")).toBe(false);expect(roleHasPermission("admin","USER_ADMIN")).toBe(true)});it("never grants service capabilities to application roles",()=>{for(const role of["operator","supervisor","manager","admin"])for(const permission of["SOURCE_DATA_MUTATE","SYNC_CONTROL"]as const)expect(roleHasPermission(role,permission)).toBe(false)});it("contains no runtime privileged email bypass",()=>{for(const file of["planning.ts","machine-load.ts","release-queue.ts","operational-scan.ts","source-data.ts"]){const source=readFileSync(resolve(__dirname,"../lib",file),"utf8");expect(source).not.toMatch(/ADMIN_EMAIL|isAuthorizedAdminEmail|felipe\.s@tankstreamdesign\.com/i)}})});
