import{StaffingLayouts}from"@/components/staffing-layouts";import{AppShell}from"@/components/app-shell";import{ScenarioBuilder}from"@/components/scenario-builder";import{capacityContext}from"@/lib/capacity";import{saveProfile}from"./actions";export default async function Page(){const d=await capacityContext(),demand=Object.fromEntries(d.loads.map((x:any)=>[x.area_code,Number(x.demand_units)]));return <AppShell><p className="eyebrow">Phase 7</p><header><div><h2>Capacity,<br/>made visible.</h2><p>Configure assumptions, compare workload and test staffing scenarios.</p></div></header><section className="capacity-strip">{d.loads.map((x:any)=><article key={x.area_code}><b>{x.area_code}</b><strong>{x.load_percent===null?"N/A":`${Number(x.load_percent).toFixed(1)}%`}</strong><span>{Number(x.demand_units).toLocaleString("en-AU")} demand / {Number(x.daily_capacity).toLocaleString("en-AU")} daily</span><small className={Number(x.capacity_gap)<0?"bad":"ok"}>{Number(x.capacity_gap).toLocaleString("en-AU")} gap · {x.relative_lead_days??"N/A"} days</small></article>)}</section><section className="panel"><h3>Create official capacity profile</h3><form action={saveProfile} className="capacity-form"><input name="name" placeholder="Profile name" required/><select name="area_id" required>{d.areas.map((a:any)=><option value={a.id} key={a.id}>{a.name}</option>)}</select><select name="shift_id" required>{d.shifts.map((s:any)=><option value={s.id} key={s.id}>{s.name}</option>)}</select><input name="resources" type="number" min="0" step=".25" placeholder="Resources" required/><input name="machines" type="number" min="0" step="1" placeholder="Machines"/><input name="hours" type="number" min=".01" max="24" step=".25" placeholder="Hours" required/><input name="rate" type="number" min="0" step=".001" placeholder="Rate / hour" required/><input name="efficiency" type="number" min="0" max="100" step=".1" placeholder="Efficiency %" required/><input name="work_days" type="number" min="0" max="7" step=".5" defaultValue="5" required/><button>Add profile</button></form></section><StaffingLayouts data={d}/><ScenarioBuilder areas={d.areas} shifts={d.shifts} demand={demand}/><section className="panel"><h3>Official profiles</h3><div className="table"><table><thead><tr><th>Name</th><th>Area</th><th>Shift</th><th>Resources</th><th>Hours</th><th>Rate</th><th>Efficiency</th><th>Daily</th><th>Weekly</th></tr></thead><tbody>{d.profiles.map((p:any)=><tr key={p.id}><td>{p.name}</td><td>{p.production_areas.code}</td><td>{p.shift_templates.name}</td><td>{p.resource_count}</td><td>{p.scheduled_hours}</td><td>{p.hourly_rate}</td><td>{Number(p.efficiency)*100}%</td><td>{(Number(p.resource_count)*Number(p.scheduled_hours)*Number(p.hourly_rate)*Number(p.efficiency)).toFixed(1)}</td><td>{(Number(p.resource_count)*Number(p.scheduled_hours)*Number(p.hourly_rate)*Number(p.efficiency)*Number(p.work_days)).toFixed(1)}</td></tr>)}</tbody></table></div></section><section className="panel"><h3>Saved scenarios</h3><div className="scenario-history">{d.scenarios.map((s:any)=><article key={s.id}><b>{s.name}</b><span>{s.capacity_scenario_lines.map((l:any)=>`${l.production_areas.code} · ${l.shift_templates.name} · ${Number(l.calculated_capacity).toFixed(1)}/day`).join(" | ")}</span><small>{s.notes||"No notes"}</small></article>)}</div></section></AppShell>}
