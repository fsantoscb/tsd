import {describe,expect,it} from "vitest";import {validatePassword} from "../lib/auth";describe("password validation",()=>{it("requires twelve characters",()=>expect(validatePassword("Short1A")).toBeTruthy());it("requires mixed characters",()=>expect(validatePassword("abcdefghijkl")).toBeTruthy());it("accepts a strong password",()=>expect(validatePassword("FactoryShift9")).toBeNull())});