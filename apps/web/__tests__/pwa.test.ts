import{describe,expect,it}from"vitest";import manifest from"../app/manifest";describe("pilot PWA",()=>{it("is installable from the production root",()=>{const value=manifest();expect(value).toMatchObject({start_url:"/",display:"standalone",theme_color:"#0b2929"});expect(value.icons?.length).toBeGreaterThan(0)});it("uses a dedicated operational identity",()=>{const value=manifest();expect(value.name).toBe("TSD Production Control");expect(value.short_name).toBe("TSD Production")})});
